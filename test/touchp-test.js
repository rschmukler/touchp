var expect = require('expect');

describe('touchp', function() {

  describe('touchp', function() {
    it('creates a non existing file');
    it('does nothing if the file already exists');
  });

  describe('touchp.sync', function() {
    it('creates a non existing file');
    it('does nothing if the file already exists');
  });
});
